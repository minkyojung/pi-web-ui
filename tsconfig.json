{
	"//": "TypeScript 7 dropped baseUrl, so paths resolve relative to this file. The browser client. Kept apart from tsconfig.server.json, which targets node. shadcn reads this one, so the @/* alias lives here.",
	"compilerOptions": {
		"target": "ES2022",
		"lib": ["ES2023", "DOM", "DOM.Iterable"],
		"module": "ESNext",
		"moduleResolution": "bundler",
		"jsx": "react-jsx",
		"strict": true,
		"noEmit": true,
		"allowJs": true,
		"skipLibCheck": true,
		"isolatedModules": true,
		"verbatimModuleSyntax": true,
		"types": ["vite/client", "node"],
		"paths": { "@/*": ["./web/src/*"] }
	},
	"include": ["web/src", "vite.config.ts"]
}
