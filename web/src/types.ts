/**
 * The wire types, as the client sees them. They live in protocol.ts at the repo
 * root, where the server reads the same file; this is only the shorter path.
 */
export type * from "../../protocol";
